# VS Code plugin for Error Central


## Development

- `npm install`
- `npm run watch` or `npm run compile`
- `F5` to start debugging

Run the `Error Central: Start Error Central session` to create the webview.
